{
  "name": "@munchi/sdk",
  "version": "1.0.22",
  "description": "",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:core": "jest --selectProject @munchi/core",
    "test:payments": "jest --selectProject @munchi/payments",
    "generate": "openapi-generator-cli generate",
    "version:sync": "pnpm -r version:sync",
    "build:workspaces": "pnpm -r build",
    "build": "pnpm build:workspaces && tsup",
    "prepare": "pnpm build"
  },
  "peerDependencies": {
    "axios": ">=1.0.0",
    "dayjs": ">=1.0.0"
  },
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.25.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^8.0.0",
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6"
  }
}
